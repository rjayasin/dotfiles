# Detach from the session with Ctrl-g (no prefix needed).
# Chosen over Alt/Meta because Termius on iOS doesn't send Meta keys.
# tmux grabs this key before the pane, so it works even inside full-screen
# TUIs like Claude Code. The default prefix + d still works too.
bind -n C-g detach
